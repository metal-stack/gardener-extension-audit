apiVersion: core.gardener.cloud/v1beta1
kind: ControllerDeployment
metadata:
  name: audit
type: helm
providerConfig:
  values:
    image:
      tag: latest
      pullPolicy: IfNotPresent

    # config:
    #   defaultBackends:
    #     clusterForwarding:
    #       enabled: true
